import React from "react";

import Header from "./Header";
import Footer from "./Footer";

const Layout = (props) => {
  return (
    <div>
      <Header />
      Layout component
      <Footer />
    </div>
  );
};

export default Layout;
